# 任务

修正下列不符合中文分类标签契约的实体 `type`：

{{invalid_types}}

# 约束

- 只把这些无效 `type` 改为语义等价、简洁的中文分类标签。
- 其他字段、值、数组顺序、关系、标识符和引文必须保持不变。
- 不得增加或删除实体、关系。

# 输出

只输出修正后的完整合法 JSON，不得输出 Markdown 或解释。输出前确认所列无效类型已全部修正，且无关内容没有变化。
