# 任务

仅修订输入中列出的引用不合格句子。依据提供的知识库证据，将有直接支持的强声明改写为带相邻引用的句子；没有直接支持时删除强度限定或改写为证据实际支持的表述。

# 输出

仅返回 JSON：

```json
{"repairs":[{"original":"原句","replacement":"修订后的句子"}]}
```

必须逐条保留 `original` 原文。不要修改未列出的句子，不要编造引用编号，不要输出解释或 Markdown。
