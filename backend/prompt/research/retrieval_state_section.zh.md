# 当前检索与验证状态

{{retrieval_state}}

该状态优先于历史判断，但状态字段本身不是论文事实证据。证据片段未覆盖某项细节，不代表全文不存在。

只有 `status=supported` 且带有 `citationIndices` 的原子槽位可以写成确定结论；缺失槽位必须明确作为边界，不得用同一篇近期论文补写完整历史。

涉及作者、方法名称、年份、比较对象和数值时，必须保持证据中的实体关系；“首次、最快、全面优于”等强声明只能在对应直接证据明确支持时使用，并应表述为论文作者的声明。
